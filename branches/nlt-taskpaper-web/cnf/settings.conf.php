<?php

// Theme Options: Set the following to a folder name in the 'thm' directory.
// This cannot be arbitrary and should match the case of the directory.
// If in doubt, leave this setting alone.
$cfg_theme = "standard";

// Add date value when applying @done tags?
// If set to true, this will add a date value when you mark a task done
// such as: @done(2008-08-08). If set to false, nothing is added to the @done tag.
$cfg_addDateToDone = true;

// do you want the file to be made writable if it isn't onupload
$auto_writable = true;

/////// Figure out how to do absolute paths...???   ////////
$cfg_taskPapers = array('txt/example.taskpaper');



?>